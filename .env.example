# Local Next.js only. Do not commit `.env.local`.
# Auth is Cloudflare Access (D1: team / AUD / access_enabled). No password login.
# Optional: CF_API_TOKEN=  (Worker Secret in production; falls back to D1)
# Optional: SETUP_TOKEN=   (protects first-boot /api/setup if set)
